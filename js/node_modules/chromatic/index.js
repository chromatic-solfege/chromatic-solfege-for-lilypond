
if ( module && module.exports ) {
	module.exports = require( './chromatic.js' );
}

if ( module.require == module.main ) {
	require( './chromatic.js' ).commandInterface( Array.prototype.slice.call( process.argv, 2) );
}
