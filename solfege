#!/usr/bin/nodejs


var lookup = require( 'chromatic' ).lookup;

var argv = Array.prototype.slice.call( process.argv );
argv.shift();
argv.shift();
console.error( lookup( argv.shift(), argv ).join( ' ' ) );



// vim: filetype=javascript :
