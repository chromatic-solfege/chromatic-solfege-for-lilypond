#!/usr/bin/nodejs


var translate = require( 'chromatic' ).translate;

var argv = Array.prototype.slice.call( process.argv );
argv.shift();
argv.shift();
console.error( translate( argv.shift(), argv ).join( ' ' ) );



// vim: filetype=javascript :
