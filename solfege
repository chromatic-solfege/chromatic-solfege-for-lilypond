#!/usr/bin/nodejs


var ch = require( 'chromatic' );

var argv = Array.prototype.slice.call( process.argv );
argv.shift();
argv.shift();
console.error( ch.transpose( argv.shift(), argv ).join( ' ' ) );



// vim: filetype=javascript :
